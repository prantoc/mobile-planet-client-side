import React from 'react';
import useTitle from '../../../hooks/useTitle';

const Dashboard = () => {
    useTitle('Dashboard')
    return (
        <>
            <h3>Dashboard</h3>
        </>
    );
};

export default Dashboard;